version: "3"
services:
  client:
    image: rooknj/lightapp2-client:latest-rpi
    container_name: client
    restart: always
    ports:
      - "80:80"
  rabbit:
    image: arm32v7/rabbitmq:3
    hostname: "myrabbit"
    container_name: rabbit
    restart: always
    ports:
      - "5672:5672"
  api:
    image: rooknj/lightapp2-api:latest-rpi
    container_name: api
    restart: always
    network_mode: "host"
    environment:
      - DEBUG="main,config,server,service"
      - RABBIT_HOST=localhost
  controller:
    image: rooknj/lightapp2-controller:latest-rpi
    container_name: controller
    restart: always
    network_mode: "host"
    environment:
      - DEBUG="main,config,service,db,pubsub,repo"
      - MQTT_HOST=localhost
      - RABBIT_HOST=localhost
  redis:
    image: arm32v7/redis:latest
    container_name: redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - ./redisData:/data
  broker:
    image: rooknj/lightapp2:broker-rpi-latest
    container_name: broker
    restart: always
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./mqtt/config:/mqtt/config
      - /mqtt/data
      - /mqtt/log
  homebridge:
    image: oznu/homebridge:raspberry-pi
    container_name: homebridge
    restart: always
    network_mode: host
    volumes:
      - ./homekit/config:/homebridge
    environment:
      - PGID=1000
      - PUID=1000
      - HOMEBRIDGE_CONFIG_UI=1
      - HOMEBRIDGE_CONFIG_UI_PORT=8080
